import { Container } from '@chakra-ui/react';
import SearchForm from '../components/Search';

const Search = () => {
  return (
    <Container mt={5}>
      <SearchForm />
    </Container>
  );
};

export default Search;
